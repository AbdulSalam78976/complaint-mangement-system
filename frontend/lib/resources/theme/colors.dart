import 'package:flutter/material.dart';

class AppPalette {
  static const Color backgroundColor = Color(
    0xFFF8F9FA,
  ); // Light neutral background
  static const Color primaryColor = Color(0xFF164B60); // Deep blue (primary)
  static const Color secondaryColor = Color(
    0xFF1B6B93,
  ); // Medium blue (secondary)
  static const Color accentColor = Color(0xFF4FC0D0); // Teal (accent)
  static const Color successColor = Color(
    0xFFA2FF86,
  ); // Mint green (success/positive)
  static const Color textColor = Color(0xFF2C2C2C); // Neutral dark text
  static const Color whiteColor = Colors.white;
  static const Color greyColor = Color(0xFF9E9E9E); // Neutral grey
  static const Color borderColor = Color(0xFFE0E0E0); // Light border
  static const Color errorColor = Color(0xFFEB5E55); // Soft red (errors/alerts)
  static const Color transparentColor = Colors.transparent;
}
